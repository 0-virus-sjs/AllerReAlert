M12 시작. M9 관리자 패널 + M8 수요 집계 + M6 AI 식단 생성 위에 진행한다.

M12 13개 Task(T-120~T-132) 진행 순서를 선행 관계 기준으로 정렬해서
표로 보여달라. OK 하면 시작.

특별 주의:
- T-120: `/admin` 라우트와 role-guard는 이미 있음 — "다이렉션 안 됨"의 정확한 재현 케이스부터 작성 후 원인 진단. 사이드바 서브경로(`/admin/users` 등)와 실제 탭 매칭, 로그인 후 admin 분기, role-guard 조기 리다이렉트 순으로 점검.
- T-121: `group_info` JSON은 보조 필드로 유지하고 `grade Int?`, `class_no String?`, `student_code String?` 컬럼 신규. 기존 데이터 마이그레이션은 NULL 허용으로 충돌 회피.
- T-124: 소속 변경 시 확정(confirmed) 알레르기는 유지하되 grade/class_no/student_code는 초기화 + "재입력 필요" 플래그를 응답에 포함. nutritionist는 호출 불가.
- T-126: 영양사 본인 소속 학교 한정 — 다른 학교 통계 노출 금지. 30분 캐시.
- T-128: NEIS `mealServiceDietInfo` 응답의 단가 필드 유무 먼저 확인. 없으면 정적 키워드→단가 seed 테이블로 fallback하고 NEIS 빈도 가중치만 사용. 데이터 출처 부재가 결정적 블로커이면 영양사 입력 단가만 사용하는 축소 모드로 후퇴.
- T-129: 입력으로 받은 영양섭취기준표(연령×성별 kcal/일) 상수화 — Generate_task.md 본 명세 표를 그대로 코드 상수로 옮긴다. 학교 분포가 비어 있을 때(M0 시드 학교 등)는 전 연령 균등 가중치로 fallback.
- T-130: **일 평균 target × 7 ± 10%** 검증은 생성 직후 후처리에서 수행. 실패 시 1회만 재요청(무한 루프 방지). 단가 제약은 AI 프롬프트에 "메뉴당 평균 X원 이내" 문구로 주입.
- T-131: `/analytics` 라우트는 `/dashboard`로 영구 리다이렉트 + NutritionistDashboardPage 컴포넌트 제거. 통합 후 데이터 페치는 React Query 키를 한 곳으로 모아 중복 호출 없게.
- T-132: 영양소 항목 추가/삭제는 동적 폼 상태로 관리(react-hook-form `useFieldArray` 권장). 기본 항목도 삭제 가능. 단가 입력 변경 시 1식당 환산값 실시간 미리보기. NEIS 학교 자동완성은 debounce 300ms.

완료 기준:
- admin 계정 로그인 → 사이드바 → `/admin` 4개 탭 모두 정상 진입 확인
- 학생 회원가입 시 학년/반/학번 입력 → 프로필에서 표시·편집 확인
- 일반 사용자가 프로필에서 학교명 조회 + "소속 변경" 흐름 성공 확인
- 영양사 대시보드 단일 화면에서 학교 정보 카드(성별/학년 분포) + 수요 집계 차트 동시 노출
- AI 식단 생성 폼: 기본 6종 영양소 자동 채움 + 항목 추가/삭제 + NEIS 학교 검색 + 월/주/일 × 평균/총합 단가 입력 모두 동작
- 생성된 주간 식단의 영양소 총합이 일 평균 target × 7의 ±10% 이내인지 후처리 검증 통과
- 단가 제약 충족 여부 확인(메뉴별 참고가격 총합 ≤ 입력 단가 한도)
