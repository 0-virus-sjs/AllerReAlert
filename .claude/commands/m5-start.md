M5 시작. M4 결과물(알레르기 등록 완료) 위에 진행한다. ⭐ 핵심 모듈 — 정확성·안정성 최우선.

M5 8개 Task(T-050~T-057) 진행 순서를 선행 관계 기준으로 정렬해서
표로 보여달라. OK 하면 시작.

특별 주의:
- T-051: 알레르기 대조 엔진 단위 테스트 커버리지 ≥ 90% 필수 (PRD §11 최우선 가치).
- T-052: 다중 인스턴스 대비 DB 기반 분산 락(Postgres advisory lock) 적용. 실패 시 지수 백오프 재시도 3회.
- T-052: 큐 인터페이스를 추상화해 추후 BullMQ + Railway Redis 마이그레이션이 가능하도록 설계.
- T-056: Service Worker 등록 + 웹 푸시 구독 동의 플로우 포함. 실시간 갱신은 폴링(30초) 우선.
- T-057: 테스트 DB는 Supabase 별도 스키마(test) 또는 별도 프로젝트로 격리.

완료 기준:
- 알레르기 등록 → 해당 식단 공개 → 이메일 + 웹 푸시 알림 수신 확인
- 공개된 식단 수정 → 변경 알림 즉시 발송 확인
- 알림 센터에서 읽음/미읽음 상태 전환 확인
- T-057 통합 테스트 3개 시나리오(알림 발송 / 변경 알림 / 미승인 미발송) 모두 green
