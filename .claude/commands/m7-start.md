M7 시작. M5 알림 엔진 + M3 대체 식단 확정 위에 진행한다.

M7 8개 Task(T-070~T-077) 진행 순서를 선행 관계 기준으로 정렬해서
표로 보여달라. OK 하면 시작.

특별 주의:
- T-070: 설문 2단계 자동 생성 — ① need_check(식사 여부) → ② menu_vote(원래 식단 + 대체 후보). 대상자는 해당 알레르기 보유자 자동 산정.
- T-072: 1인 1표, 마감 전까지 변경 가능(upsert). 마감 후 변경 불가 — 마감 시각 체크 필수 (PRD §11.4).
- T-073: node-cron 5분 주기 폴링으로 마감 시각 도래 설문 자동 close → 결과 집계 → 캐시 무효화.
- T-074: 설문 생성 즉시 참여 알림 + 마감 24시간/2시간 전 미응답자 리마인더.
- T-077: E2E 대상은 Vercel Preview + Railway Staging URL.

완료 기준:
- 대체 식단 확정 → 설문 자동 생성 → 대상자 알림 발송 확인
- 이용자 투표 → 결과 반영 → 마감 후 변경 불가 확인
- 마감 후 영양사 대시보드에서 참여율·메뉴별 득표 확인
- T-077 E2E 시나리오 green
